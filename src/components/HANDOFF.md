1. Exposes: `src/app/page.tsx` (renders `<DemoApp/>`), client components `DemoApp`, `OutcomeLedger`, `ScoreGauge`, shared `outcomePresentation.ts` (classification→color/label/penalty), and `mockAudit` (a contract-validated `AuditResult` for NCT01951625). Swap `mockAudit` for a real audit result of the same shape to go live.
2. `<OutcomeLedger audit={AuditResult} intervalMs?={220} onReveal?={(match,i,total)=>...} onComplete?={()=>...} />` streams one row per `OutcomeMatch`; `<ScoreGauge score={number 0-100} caption?={string} />` tweens its big number/arc toward `score` — feed it a running score and snap it to `audit.integrityScore` in `onComplete`.
3. Demo button (and typing NCT01951625) loads `mockAudit`; other valid NCT ids show a "coming soon" state (no fake API). Rows reveal on a `setInterval` timer (green→red→demoted→added order); each landed row calls `onReveal`, `DemoApp` subtracts that classification's penalty from a running score starting at 100, then settles on `audit.integrityScore` (derived from `@/lib/report/score` `computeIntegrityScore`, so gauge/letter/scorer agree).
